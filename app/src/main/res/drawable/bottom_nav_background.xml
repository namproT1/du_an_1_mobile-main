<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Gradient nền -->
   <solid android:color="#56B0A6"/>


    <stroke android:color="@color/black" android:width="1dp"/>

    <!-- Padding bên trong (nếu cần) -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="5dp"
        android:bottom="5dp" />
</shape>
